function optimized = optimize_points(distribution, extracted_points)

% In the default case, we'll just use the vanilla array.
% Some distributions may need to do further transformations.
optimized = extracted_points;
